export class Bids {
  bidId: number;
  amount: number;
  description: string;
  requisitionId: number;
  supplierId: number;
  // tslint:disable-next-line:variable-name
  transport_cost: number;
  status: string;
}
